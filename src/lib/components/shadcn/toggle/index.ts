import Root from './Toggle.svelte';

export { Root, Root as Toggle };
export {
	toggleVariants,
	type ToggleProps,
	type ToggleIntent,
	type ToggleSize,
	TOGGLE_INTENTS,
	TOGGLE_SIZES,
} from './toggle-variants.js';
